import { PostPaginationDetail } from './post-pagination-detail';

describe('PostPaginationDetail', () => {
  it('should create an instance', () => {
    expect(new PostPaginationDetail()).toBeTruthy();
  });
});
