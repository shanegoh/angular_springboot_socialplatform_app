import { AccountPaginationDetail } from './account-pagination-detail';

describe('AccountPaginationDetail', () => {
  it('should create an instance', () => {
    expect(new AccountPaginationDetail()).toBeTruthy();
  });
});
